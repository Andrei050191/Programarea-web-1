// arrow function
const checkEvenOdd = (number) => {
  return (number % 2 === 0) ? "par" : "impar";
};

// apelare funcție
let result = checkEvenOdd(7);

// afișare rezultat
console.log("Numărul este:", result);
